<%= form_with model: post, local: true do |f| %>
  <div>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary mt-4" %>
<% end %>
